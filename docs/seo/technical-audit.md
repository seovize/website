# Seovize — Technical SEO Audit (Phase 8)

All findings below are from live GSC data pulled this session (2026-08-01) or direct inspection of the current codebase — nothing is assumed from memory of prior sessions without re-verification.

| Area | Evidence | Affected URL/file | Impact | Proposed change | Validation | Rollback |
|---|---|---|---|---|---|---|
| **Sitewide duplicate `<title>` suffix** | Discovered during Phase 10 validation, not the original Phase 8 pass: `app/layout.tsx` sets `title.template = "%s | Seovize"`, but ~20 individual pages' own `metadata.title` (and 6 entries in `lib/industry-data.ts`'s `metaTitle`) also ended in literal `" | Seovize"`, so the rendered `<title>` read e.g. "SEO Services Austin Texas \| Seovize \| Seovize". Verified by grepping the built static HTML in `.next/server/app/**/*.html` for every route, before and after the fix | ~20 page files across `app/` + `lib/industry-data.ts` (Dallas/Austin/Houston/San Antonio SEO, all 6 city social-media pages, `/services/*` ×6, `/industries`, `/industries/[slug]` ×6, `/locations`, `/locations/texas`, `/compare/seo-agency-vs-freelancer`, `/privacy-policy`, `/pricing`, the research report) | A real, live, sitewide SERP-snippet defect — every affected page's title tag as shown in Google search results carried a redundant, slightly unprofessional-looking "\| Seovize \| Seovize" | **Fixed this phase**: removed the trailing `" | Seovize"` from each page's own `metadata.title` (and industry `metaTitle`), leaving the layout's template as the single source of the brand suffix. `openGraph.title` fields were left untouched — those aren't run through the title template, so their own "\| Seovize" is correct and non-duplicated | Rebuilt (`next build`) and grepped every generated static HTML file's `<title>` tag for the string "Seovize" appearing twice — zero literal-duplicate matches remain (two pages, `/about` and `/our-process`, legitimately mention "Seovize" once mid-title as a natural word, which is fine) | Re-add the removed `" | Seovize"` suffix per file if this diagnosis is disputed |
| **Site-wide indexation** | GSC Page Indexing report (live pull): 8 of 48 known pages indexed, 40 not indexed | Site-wide | Most pages, including all 4 Texas SEO city pages except Dallas, have zero chance of ranking while unindexed | Requested indexing this session for Austin, Houston, San Antonio SEO pages (Dallas requested in Sprint 2.1). Broader fix is patience + continued internal linking + sitemap freshness — indexing requests are a nudge, not a guarantee | Re-check GSC Page Indexing report in 1–2 weeks | None needed — indexing requests are non-destructive |
| **Crawled — currently not indexed** | GSC: 16 pages in this bucket, "Failed" validation, source = Google systems | Unspecified (aggregate count only — GSC's per-URL breakdown wasn't drilled into this session) | Google chose not to index these after crawling — typically a low-authority/thin-differentiation signal for a newer domain, not a technical error | No code fix applicable; this resolves as domain authority and content depth compound. Owner should periodically re-check the itemized list in GSC → Indexing → Pages → "Crawled – currently not indexed" | Re-check monthly | N/A |
| **Not found (404)** | GSC: 8 pages, "Failed" validation | Likely includes `/dallas-seo-company/` and other legacy WordPress URLs — confirmed via live URL Inspection this session that `/dallas-seo-company/` now redirects correctly (301 → `/locations/texas/dallas-seo`), but Google's last actual crawl (Jun 24, 2026) predates the fix and still shows 404 | Stale GSC data, not a live problem, for the URL checked. The other 7 in this bucket weren't individually inspected this session | Requested a live re-test on `/dallas-seo-company/` (confirmed working); recommend the owner spot-check the remaining 7 via GSC's itemized list | Re-check after Google's next recrawl | N/A |
| **Page with redirect** | GSC: 3 pages, validation "Not Started" | Legacy WordPress URLs with real 301s in `next.config.ts` (`/seo`, `/services`, `/wordpress-website-design`, blog post slugs, etc.) | Expected, healthy — redirects correctly excluded from the index in favor of their targets | None — this is the correct behavior | N/A | N/A |
| **Blocked by robots.txt** | GSC: 10 pages | `app/robots.ts` only disallows `/api/` (3 routes: `/api/contact`, `/api/report-signup`, `/api/cron/nurture`) — doesn't account for all 10 | Likely Google's stale memory of the *old WordPress site's* robots.txt (which typically blocks `/wp-admin/`, `/wp-content/`), not the current Next.js robots.txt. `next.config.ts` does have 302 redirects for `/wp-content/:path*`, `/wp-admin/:path*`, `/wp-login.php` → `/`, suggesting these are exactly the legacy paths in question | No code change needed if this theory holds — will self-resolve as Google recrawls and updates its robots.txt cache. Owner should verify by checking the itemized URL list in GSC | Check itemized list in GSC → Indexing → Pages | N/A |
| **Blocked due to access forbidden (403)** | GSC: 2 pages | Not identified this session | Unknown — could be a real Vercel deployment-protection leak or another stale legacy record | **Needs owner follow-up**: check the itemized 2 URLs in GSC directly; if either is a real production URL returning 403, that's an active bug worth an emergency fix | Check itemized list; if real, test the URL directly | N/A |
| **Excluded by 'noindex' tag** | GSC: 1 page | `/case-studies` (`robots: { index: false }` added in Sprint 2.1) | Intentional — Option A de-surfacing of the placeholder case-studies page | None — correct as-is | Confirmed via build output and page source | Remove the noindex once real case studies exist |
| **US search visibility is one query cluster deep** | GSC Performance, US-filtered, 3 months: 193 impressions, 0 clicks, avg. position 77.9, all 18 recorded queries are Dallas-SEO variants | Site-wide | Every non-Dallas page (Austin, Houston, San Antonio SEO/social pages, both national service pages) has zero recorded US impressions — consistent with the indexation gap above, not necessarily lack of demand | Addressed via this session's indexing requests; also flags that **any future ranking-tracking tool should watch Austin/Houston/San Antonio/national-service query sets specifically**, since there's no current baseline for them | Re-check GSC Performance (US filter) in 3–4 weeks | N/A |
| **Redirect chains** | Manual review of `next.config.ts` | `next.config.ts` | No chains found — every redirect rule points directly to a live, canonical destination (no redirect → redirect → destination patterns) | None needed | Reviewed all 20 rules directly | N/A |
| **Sitemap accuracy** | GSC Sitemaps report: `/sitemap.xml` submitted Jul 1, last read Jul 4, status Success, 45 pages discovered. `app/sitemap.ts` currently lists more routes than 45 (case-studies removed from it in Sprint 2.1, San Antonio SEO already present) | `app/sitemap.ts` | Sitemap hasn't been re-read by Google since Jul 4 — predates all of Sprint 2.1 and this phase's changes | No code change (sitemap.ts already accurate); recommend the owner resubmit the sitemap in GSC after this PR deploys to Preview/production, to prompt a fresh read | Verify sitemap URL list via `next build` output (59 routes generated, matches expectations) | N/A |
| **Duplicate titles** | `npm run build` route list cross-referenced against corrected metadata (Sprint 2.1 + this phase) | Dallas/Austin/Houston/San Antonio SEO pages | Previously all four had "Top [City] SEO Company" in the `<title>`; Sprint 2.1 fixed Dallas only, this phase fixed Austin/Houston/San Antonio to match — titles are now distinct per city, no duplication | Already fixed this phase | `grep -r "Top.*SEO Company" app/` returns no matches post-fix | N/A |
| **Orphan pages** | Cross-referenced `relatedPages` arrays + nav + sitemap against the 59-route build output | None found | `/case-studies` was previously nav-linked but is now intentionally de-surfaced (not orphaned — reachable via footer redirects and direct URL, correctly noindexed) | None needed | Manual link-graph check via `relatedPages`/Footer/homepage service grid | N/A |
| **Core Web Vitals / image optimization** | Not independently re-measured this session (no Lighthouse/PageSpeed tool run) | `/services/website-design` claims "Core Web Vitals 90+" | Claim is a stated build standard (Next.js + Vercel edge), not a fabricated statistic, but wasn't verified live this session | **Deferred** — recommend running a live PageSpeed Insights check against the production domain before the next claim-accuracy pass | N/A this session | N/A |
| **Mobile usability / forms / conversion tracking** | Not independently tested this session (would require Preview deployment + browser testing) | `/api/contact`, `/api/report-signup` | Both routes have full test coverage (Sprint 2.1: 17 tests) confirming logic correctness; live mobile-browser click-through wasn't re-tested this specific session | Covered by Phase 10's validation pass once this branch reaches Preview | Preview deploy + manual mobile test | N/A |
| **Preview-domain protection** | Verified in Sprint 1/2 (prior sessions) — not re-checked this session | Vercel project settings | No new changes to this area this phase | None | N/A this session | N/A |
